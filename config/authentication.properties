owndb {
  enabled = true
  users {
    user1 {
      userId = "jenkins"
      path = "/var/jenkins_home/.ssh/password.txt"
    }
    user2 {
      userId = "denis"
      path = "/var/jenkins_home/.ssh/password.txt"
    }
  }
}

scriptSecurity {
  jobDsl = false
}
